Хочу, чтобы команда prompt-distil distill показывала понятный прогресс выполнения — не просто одну строчку “Calling the model…”, а нормальные статусы, которые отражают, чем сейчас занят дистиллятор. Надо сделать это максимально просто: пусть будет глобальный объект reporter, у которого можно один раз установить status.update() из rich, а потом в любом месте кода звать reporter.step("текст шага"). Таким образом, не надо будет передавать status или console в каждую функцию.

Также хочу, чтобы в main.py убрать бесполезные with console.status(...), которые сейчас ничего не делают. Пусть там только один with console.status("...") будет — и он будет использоваться именно для репортера. Все остальные статусы пусть идут из внутренних функций через reporter.step(). Асинхронность не вовлекаем, просто сделать аккуратный, тестоустойчивый патч.
